#include "s21_decimal.h"

int s21_from_decimal_to_int(s21_decimal src, int *dst) {
    // int sign = s21_get_bit(&src, 127);
    // int sign1 = s21_get_bit(&src, 31);
    // if (src.bits[0] <= MAX_UN_INT){
    //   *dst = src.bits[0];
    //   if (sign || sign1){
    //     *dst *= -1;
    //   } return *dst;
    // }
    return 0;
}

